Créer une page admin ou seul les utilisateurs avec le rôle "admin" role id 3 peuvent accéder. Il faut afficher toutes les images uploadées par les utilisateurs avec un bouton supprimer à côté de chaque image. Le nombre total d'images ainsi que le poids total en Mo doivent être affichés en haut de la page.

Dans la page @front/src/pages/Group.tsx dans la liste des membres et sur chaque poste afficher l'image de profil de l'utilisateur

Dans le backend, delete_groupmember ajouter une sécurité pour que seul les admins du groupe ou le créateur du groupe puisse supprimer un membre du groupe.